[project]
name = "duckets"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
    "hyperliquid-python-sdk>=0.16.0",
    "eth-account>=0.13.0",
]

[tool.setuptools.packages.find]
include = ["app*"]